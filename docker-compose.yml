version: "3.2"
services:
  db:
    image: postgres
    ports:
      - "5433:5432"
    environment:
      - POSTGRES_DB=postgresql
      - POSTGRES_USER=postgresql
      - POSTGRES_PASSWORD=postgresql

  javavulny:
    build: .
    image: nightvision/javavulny:latest
    container_name: javavulny
    environment:
      SPRING_PROFILES_ACTIVE: postgresql
    ports:
      - "9000:9000"
    links:
      - db
    depends_on:
      - db
